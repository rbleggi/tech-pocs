pipeline {
    agent any
    stages {
        stage('Provision Infra') {
            steps {
                dir('infra') {
                    bat 'tofu init'
                    bat 'tofu apply -auto-approve'
                }
            }
        }
    }
}
